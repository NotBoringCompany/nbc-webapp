import Layout from "@/components/Layout/Layout";

const Inventory = () => {
    return (
        <Layout
            withAuth
            pageTitle='Inventory'
        >

        </Layout>
    )
}

export default Inventory;