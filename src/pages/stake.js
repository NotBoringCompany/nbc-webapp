import NavbarMenu from "@/components/Dropdowns/NavbarMenu";
import MainNavbar from "@/components/Navbar/Navbar";

export default function Stake() {
    return (
        <div>
            asd
        </div>
    );
}

export async function getServerSideProps(ctx){
    return {
        props:{
            data:null
        }
    }
}
