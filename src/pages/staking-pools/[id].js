import { useRouter } from 'next/router'

const StakingPool = () => {
    const router = useRouter();
    return (
        <div>
            <h1>Staking Pool</h1>
        </div>
    )
}

export default StakingPool